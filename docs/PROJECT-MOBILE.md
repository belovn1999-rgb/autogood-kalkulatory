# AUTOGOOD · Mobile.de / Otomoto — документ проекта

> **Единый источник правды** для всех чатов (Claude Code, Codex / ChatGPT).
> Прочитай перед работой над `mobile.html`, обнови после каждого изменения
> (раздел «Журнал изменений» + статусы в «Бэклоге»). Язык документа — русский,
> идентификаторы кода — как в коде.

- **Страница:** https://belovn1999-rgb.github.io/autogood-kalkulatory/mobile.html
- **Репозиторий:** https://github.com/belovn1999-rgb/autogood-kalkulatory (ветка `main` = продакшн, GitHub Pages)
- **Локальная копия (основная):** `/Users/nikitq/cloude/autogood-kalkulatory`
- **Владелец продукта:** Никита (AUTOGOOD). Тестирует на продакшне.
- **Куратор изменений:** чат Claude Code «mobile.de / otomoto» (этот документ ведёт он).
- **Последнее обновление документа:** 2026-09-26

---

## 1. Цель

Инструмент менеджера AUTOGOOD для подбора и оценки авто для клиента
(физлицо, всегда платит **брутто**):

1. **Поиск:** один набор фильтров → правильный поиск на **mobile.de** (Германия/ЕС)
   и **otomoto.pl** (Польша) для любой марки и модели.
2. **Распознавание объявления:** ссылка mobile.de или otomoto.pl → данные авто
   заполняют форму, считаются доставка/осмотр/акциз для калькулятора.
3. **Анализ рынка:** реальные цены предложений обоих рынков на одном графике,
   статистика, место конкретного авто на рынке, история цен по датам.
4. **Цель следующего этапа:** «**Привезти из Германии под заказ vs купить в
   Польше**» — цена mobile.de «под ключ в PL» (логика калькулятора) против цен
   otomoto, короткая сводка для клиента «где выгоднее» на конкретных данных.

Критерий качества: каждое поле формы корректно переносится на оба сайта для
каждой марки и модели; цифры анализа совпадают с тем, что видно на сайтах;
расчёт «под ключ» совпадает с калькулятором до злотого.

## 2. Правила работы для всех чатов (AI working agreement)

1. **Одно место для кода:** `/Users/nikitq/cloude/autogood-kalkulatory`
   (или временный клон, который **в тот же день** коммитится и пушится в
   `main`). Не оставлять правки незакоммиченными в других папках: всё, что не в
   `origin/main`, считается потерянным для остальных чатов.
   Перед работой: `git fetch && git status -sb`, `git pull --rebase origin main`.
   Параллельно работают несколько агентов — всегда начинай со свежего `main`.
2. **Прочитай этот документ** (разделы 4–7) и файлы, которые меняешь.
3. **Маленькие коммиты, сразу push.** Долгие незакоммиченные правки в
   `src/mobile-market-analysis.js` почти гарантируют конфликт.
4. **Кэш:** при каждом изменении файла поднимай `?v=` у его `<script>/<link>` в
   `mobile.html` (уникальная строка, напр. `feature-YYYYMMDD`). Конфликты при
   rebase чаще всего только в этих строках — бери любую новую версию.
5. **Проверка:** локальный сервер (`python3 -m http.server <порт>` из корня),
   проверка в браузере (DOM/консоль), затем push и проверка продакшна
   (GitHub Pages обновляется ~1 мин). Node в окружении нет.
6. **Визуальные изменения крупнее мелочи — сначала варианты владельцу,**
   он выбирает. Баги — чинить сразу и выкладывать, если не ломает работу.
7. **Не перетирай чужие решения молча.** Если меняешь поведение, введённое
   другим агентом (см. «Решения»), — запиши причину в журнал.
8. **После изменения:** добавь строку в «Журнал изменений» (дата, агент,
   что и зачем, коммит), обнови статус в «Бэклоге», новые решения — в «Решения».
9. Коммиты: понятное сообщение «что и зачем»; подпись агента по его правилам.

## 3. Архитектура

Статический сайт (GitHub Pages), без сборки.

| Файл | Роль |
|---|---|
| `mobile.html` | страница: карточка ссылки, форма фильтров, панель «Aktualne oferty», анализ рынка, история |
| `src/mobile.js` | форма, каталоги, построение URL mobile.de / otomoto, распознавание ссылок, тарифы доставки, классы акциза, ссылки в калькулятор |
| `src/mobile-market-analysis.js` | анализ рынка: загрузка otomoto, график, статистика, история поисков, избранное, история цен, отчёт (картинка/PDF) |
| `src/mobile-bridge.js` | мост с закладкой mobile.de (postMessage / `#autogood-import=`), проверка и очистка данных |
| `src/autogood-bookmarklet.js` | код закладки «AUTOGOOD ↦ mobile.de» (собирается в `javascript:` на лету) |
| `src/turnkey-estimate.js` | расчёт «под ключ» (копия формулы калькулятора) + живой курс Walutomat |
| `src/mobile-model-catalog.generated.js` | каталог марок/моделей + числовые ID mobile.de (`tools/generate-mobile-de-ids.py`) |
| `src/otomoto-catalog.generated.js` | каталог otomoto (`scripts/generate-otomoto-catalog.mjs`) |
| `src/main.jsx` + `src/main.compiled.js` | калькуляторы (`calculators.html`); JSX правится вручную в обоих файлах |
| `server/mobilede-import.mjs` | локальный сервер распознавания mobile.de через настоящий Chrome (CDP), наружу через Cloudflare tunnel |
| `data/exchange-rates.json` | запасной курс EUR/PLN (может быть устаревшим — основной курс живой) |
| `docs/turnkey-market-comparison.md` | детальный план «под ключ vs Польша» |
| `docs/mobile-de-filter-acceptance.md`, `docs/mobile-market-import.md` | приёмка фильтров, формат импорта |

## 4. Механизмы (как это работает)

### 4.1 Поиск mobile.de
- URL `suchen.mobile.de/fahrzeuge/search.html` с **числовыми ID**
  `ms=<make>;<model>;<group>;<text>`. SEO-маршрут `/auto/…` теряет все фильтры.
- Поле «Wersja» — свободный текст (напр. «S line»), уходит 4-м сегментом `ms`.
  У otomoto аналога нет.
- Контракт параметров встроен в HTML страницы поиска (`"blt":{"type"`).
  Неверный параметр молча игнорируется. Проверка: `numResultsTotal`.

### 4.2 Поиск otomoto
- `otomoto.pl/osobowe/<make>/<model>?search[...]`, модели через
  `matchedOtomotoModels` / `otomotoModelAliases` / `otomotoModelFallback`.
- Цена фильтра переводится EUR→PLN по текущему курсу.
- Страна продавца **не** отправляется как `filter_enum_country_origin`
  (другое значение, резало выдачу).

### 4.3 Распознавание ссылки
- **mobile.de:** «Rozpoznaj» открывает объявление в новой вкладке и пробует
  сервер (`readMobileDeApiUrl`, сейчас Cloudflare quick tunnel — адрес
  временный). Если сервера нет — пользователь жмёт закладку на странице
  объявления, данные приходят через `mobile-bridge.js`.
- **otomoto.pl:** переключатель mobile.de / otomoto.pl у поля ссылки
  (переключается сам по адресу). Объявление читается через прокси
  `r.jina.ai` (`x-respond-with: html`) из `__NEXT_DATA__ → advert`.
  Цена в PLN (+ экв. EUR), без доставки из Германии.
- Распознанное авто заполняет форму. Диапазоны сравнения: год — точный
  (правка Codex `adef7fd`), мощность ±10%, объём ±100, пробег свободный.
  Это авто отмечается ромбом на графике анализа.

### 4.4 Закладка «AUTOGOOD ↦ mobile.de»
- mobile.de блокирует серверы и прокси (403), но не браузер пользователя.
- На объявлении: JSON-LD `@type: Car` + `data-testid` → данные авто.
- На списке: Next.js flight data (`self.__next_f`, двойное кодирование),
  20 предложений на страницу, максимум 100 страниц на сортировку. Выборка — 8
  страниц равномерно по списку, дальний конец читается через `od=down`,
  место = `total − позиция`. Полное название — из `h2` карточки. Для «под
  ключ» сохраняются страна, индекс, город, кузов, объём, топливо, мощность.
- Доставка данных: `window.opener.postMessage` (если открыто из AUTOGOOD)
  или `mobile.html#autogood-import=<json>`. Приём только от
  `https://(suchen|www|m).mobile.de`.
- **Закладка — копия кода на момент перетаскивания.** После изменений
  закладки её надо перетащить заново.

### 4.5 Анализ рынка
- **otomoto:** 8 страниц по 32 предложения равномерно по списку от дешёвых
  к дорогим (3 параллельно, через `r.jina.ai`). У каждого предложения есть
  реальное место в списке (`rank`, `marketTotal`).
- **mobile.de:** через закладку («Pobierz z mobile.de ↗»).
- **График:** otomoto синий, mobile.de оранжевый, переключатели источников.
  Оси: место в списке / пробег / год; медиана и P25/P75 подписаны.
  Точки открывают объявления; во всплывающей подсказке полное название.
  Смешанный вид — в PLN, только mobile.de — в EUR.
- **Статистика:** количество, мин, макс, медиана, типичный диапазон P25–P75,
  по источникам.
- **Подозрительные предложения** (вне статистики и истории, серые пустые
  кружки на графике):
  - слова «uszkodzony, na części, do naprawy, po wypadku, cesja, przejęcie
    najmu/leasingu, Motorschaden, Unfall…» (без «bezwypadkowy», «unfallfrei»,
    «możliwy leasing»);
  - цена < 1/3 или > 3× медианы **машин того же года ±1**.
- **Сравнение цены:** поле «Porównaj cenę (EUR)» ставит ромб на график.
- **Карточки:** избранное → панель инструментов (ссылки на поиск, обновить,
  картинка, PDF) → «Parametry poszukiwania» → «Statystyki» → «Rozkład cen» →
  «Aktualne oferty» → «Historia cen». Во время загрузки — каркас карточек.
- **Отчёт для клиента:** копия картинкой и PDF (Codex, `html-to-image`).

### 4.6 История поисков, избранное, история цен
- Каждый поиск логируется (последние 20). Избранное (★) хранится всегда,
  показывается вверху анализа «Ulubione auta»; шаг «3 Analiza» открывает
  анализ или избранное.
- Выбранная запись истории + изменённые фильтры → зелёная ✓ сохраняет
  изменения в эту же запись.
- **История цен:** каждая проверка (открытие поиска с загрузкой или
  «Odśwież dane») = **новая строка** с датой и временем. otomoto в PLN,
  mobile.de в EUR (курс не искажает динамику). Для каждой строки: количество,
  мин, P25, медиана, P75, макс, стрелки изменения к прошлой проверке
  (▲ зелёная / ▼ красная), мин и макс — ссылки на эти объявления.
  Предложения mobile.de в течение 2 ч после проверки добавляются к ней.
- Хранение: `localStorage` браузера (`autogood.mobile.marketHistory.v2`) —
  у каждого браузера своя история.

### 4.7 Курс и «под ключ»
- Курс = курс калькулятора: **Walutomat, лучшая цена продажи EUR→PLN**
  (`AUTOGOOD_TURNKEY.calculatorRate()`), запасной — `data/exchange-rates.json`.
- `turnkeyDirect()` = формула вкладки «Zakup bezpośredni» калькулятора:
  авто×курс + осмотр и транспорт (+VAT 23%) + акциз по классу двигателя
  + комиссия AUTOGOOD (1829,27 + 1%, +VAT) + TO 150 + перевод 250.
  **Без регистрации, без скидки**, итог округляется до 50 PLN.
  Сверено с калькулятором.
- `turnkeyForListing()` берёт тариф доставки/осмотра по стране и индексу
  продавца и класс акциза по топливу/объёму.
- **Константы — копия `src/main.jsx`. Меняешь калькулятор — меняй и модуль.**

## 5. Решения (журнал решений)

| Дата | Решение | Кто |
|---|---|---|
| 09-21 | mobile.de только по числовым ID моделей | Claude |
| 09-21 | Страна продавца не отправляется в otomoto | Claude |
| 09-24 | Цвета: otomoto синий `#1d5fd0`, mobile.de оранжевый `#f56a00` | владелец |
| 09-24 | mobile.de читается закладкой в браузере пользователя (сервера нет) | Claude |
| 09-25 | Все предложения остаются на графике; типичный диапазон = P25–P75 | Codex |
| 09-25 | Точки графика снова открывают объявления | владелец |
| 09-26 | Подозрительные цены: видны серыми, не входят в статистику | владелец |
| 09-26 | Всё считается для физлица, сравниваем только брутто | владелец |
| 09-26 | «Под ключ»: без регистрации, без скидок, только цена объявления | владелец |
| 09-26 | Курс везде = живой курс калькулятора (Walutomat) | Claude |
| 09-26 | История: 1 нажатие «Odśwież dane» = 1 строка; автоматизация позже | владелец |

## 6. Бэклог

Статусы: ✅ сделано · 🔄 в работе · ⏳ запланировано · 💤 отложено · ❓ ждёт решения

| # | Задача | Статус | Кто |
|---|---|---|---|
| B1 | Цена «под ключ» у каждого предложения mobile.de (переключатель на графике, всплывающая подсказка, медиана под ключ в истории) — план: `docs/turnkey-market-comparison.md` | ⏳ база готова | Claude |
| B2 | Сводка для клиента «Привезти из Германии vs купить в Польше» | ⏳ | — |
| B3 | Конкретное авто mobile.de: под ключ на фоне otomoto | ⏳ | — |
| B4 | п.11: график медианы по датам (данные уже пишутся) | ⏳ | — |
| B5 | Автоматическая проверка избранного раз в день (нужен сервер) | 💤 | — |
| B6 | Постоянный сервер распознавания mobile.de (Cloudflare), тестирует владелец отдельно | 🔄 | Codex / владелец |
| B7 | Удалить кнопку «Usuń zaimportowane oferty» (сейчас мало полезна) | ❓ | — |
| B8 | Ожидаемая цена авто по году/пробегу/мощности (модель цены) | ⏳ | — |
| B9 | Расчёт «Ścieżki zakupu» для авто с otomoto (покупка в PL) | ❓ | — |
| B10 | Мобильная версия (телефон) | 💤 не нужно пока | — |
| B11 | Свой прокси вместо `r.jina.ai` (на Cloudflare) | ⏳ | — |
| B12 | `data/exchange-rates.json` устарел (30.06) — обновлять или убрать зависимость | ⏳ | — |

## 7. Журнал изменений (новые сверху)

| Дата | Агент | Изменение | Коммит |
|---|---|---|---|
| 09-26 | Claude | Аудит правок вне `main` во всех копиях (раздел 8), правило «одно место для кода» | — |
| 09-26 | Claude | «Aktualne oferty» — отдельная карточка; иконки в заголовках; каркас загрузки; «под ключ» без регистрации | 41ec5cd |
| 09-26 | Codex | Анализ: только переключатели площадок, облегчённые заголовки, одна панель инструментов | 2c5f6fd |
| 09-26 | Claude | 1 проверка = 1 строка истории; подозрительные предложения вне статистики; `turnkey-estimate.js`; живой курс Walutomat | d73cbde |
| 09-26 | Codex | Отчёт клиенту: картинка, PDF, выбор площадок, статистика по источникам | 52d37b7, 86256e2 |
| 09-26 | Codex | Распознавание оснащения mobile.de (DE), заголовок объявления, камера в названии, фильтры комфорта | 467f6ce, 01482c5, fac7416, cf726c9 |
| 09-26 | Codex | Импорт объявления в карточке ссылки; точный год в фильтрах сравнения; единый лист параметров; логотипы площадок | adef7fd, 260988a |
| 09-25 | Claude | Распознавание ссылок otomoto.pl + переключатель | 94dea1b |
| 09-25 | Claude | Paliwo на всю ширину, без внутренних рамок полей | 916f52d |
| 09-25 | Claude | Анализ карточками, полные названия, шаги 1–3 кликабельны | 48ea899, d11d1f8 |
| 09-25 | Claude | Избранное в анализе, история цен с датами | 99af641 |
| 09-25 | Codex | Квартили P25–P75 по всем предложениям | 1323268 |
| 09-25 | Codex | Сервер импорта через тестовый туннель; сводка объявления в карточке ссылки | 616c7cf, d9de053 |
| 09-25 | Claude | ✓ сохранение изменений в запись истории; шапка формы в колонке карточек; обе кнопки анализа работают | 7173a09, 4c3e87c, 9d7d975 |
| 09-24 | Claude | Закладка mobile.de, mobile.de на графике, маркер «To auto», вердикты | 19d7905, 7b9ff5c |
| 09-24 | Claude | График по площадкам, реальное место в списке; выбросы в одной валюте | e775e67, 1a243e6 |
| 09-23 | Claude/Codex | Анализ по живым предложениям otomoto; история поисков и избранное; глобальная навигация | 0a48622, f19e58f, 83faf21 |
| 09-21 | Claude | Числовые ID mobile.de; маппинг otomoto; раскладка фильтров карточками | 1aad552, cc337f2, 16b4f96 |

## 8. Незавершённые изменения вне `main` (аудит 2026-09-26 15:30)

`origin/main` и основная копия `~/cloude/autogood-kalkulatory` синхронны; ни в
одной ветке репозитория нет правок mobile.de, которых нет в `main`. Временные
клоны Codex `/private/tmp/autogood-*` смержены, кроме одного. **Вне `main`:**

| Где | Что | Статус |
|---|---|---|
| Основная копия, `server/mobilede-import.mjs` (не закоммичено, 15:31) | Codex переносит сюда рабочую версию сервера распознавания | 🔄 Codex: закоммитить и запушить |
| `/private/tmp/autogood-mobile-align` (без ветки, 14:26) | Кнопка «Zapisz» формы как переключатель избранного ★; иконка авто в «Parametry poszukiwania» | ❓ владельцу: нужно ли |
| `~/Documents/Codex/2026-05-13/general-requirements-…` (старая копия Codex, отстаёт на 85 коммитов) | **Сервер распознавания mobile.de**: `server/mobilede-import.mjs`, `server/load-local-env.mjs`. **Этот код сейчас работает** на `127.0.0.1:8788` за Cloudflare tunnel («Rozpoznaj») | ❗ перенести в `main` (см. строку 1) |
| там же | Черновики сервера 01–03.09: `mobilede-auto-api*`, `mobilede-piloterr*` (платный API), `mobilede-file-import*`, `mobilede-url*` + тесты | ❓ оставить/удалить |
| там же | UI-черновик графика от 25.09 (устарел, идеи уже в `main`) | закрыто, не переносить |
| там же | Коммит `bdf97c4` VIN: BMW через плитку каталога (не пушен с 09.09) — **другой проект (VIN)** | ❓ владельцу |
| там же | Правки PDF и договора (`pdf*.js`, `sale-contract*`, `umowa-sprzedazy.html`, 08–09.09) — **другой проект** | ❓ владельцу |

Копии всех этих правок (патчи и файлы) лежат в
`~/Documents/ChatGPT/MOBILE.DE/hanging-work-2026-09-26/`. Ничего не удалено.

Запущенные серверы mobile.de (26.09): `:8788` — из старой копии Codex (за
туннелем); `:8789` — из `/private/tmp/autogood-mobile-inline-mapping-20260926`;
`:8791` — из основной копии.

## 9. Риски и ограничения

- **Параллельные правки** одного файла несколькими агентами → конфликты;
  см. правила в разделе 2.
- `r.jina.ai` — бесплатный внешний прокси: ограничения/отказ = нет данных otomoto.
- Сервер mobile.de на quick tunnel — адрес меняется при каждом запуске.
- Закладку нужно перетаскивать заново после изменений.
- История в `localStorage`: не переносится между браузерами/устройствами.
- Цены объявлений ≠ цены сделок.

## 10. Глоссарий

- **Под ключ (pod klucz)** — стоимость авто, доставленного и готового в PL
  для физлица, по формуле «Zakup bezpośredni», без регистрации.
- **P25 / медиана / P75** — цена, ниже которой 25% / 50% / 75% предложений.
- **Проверка** — одна загрузка цен рынка (строка в «Historia cen»).
- **Закладка** — bookmarklet «AUTOGOOD ↦ mobile.de».
